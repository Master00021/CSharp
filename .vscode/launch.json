/*
    Este archivo se encarga de contener todas las configuraciones del programa.
    El atributo 'preLaunchTask' define que es lo que se hará antes de que se ejecute el programa.
    El valor "dotnet: build", hará que se haga una build siempre, antes de que se ejecute el programa.
    El atributo "console", determina que tipo de consola se utilizará en el programa.
    El valor "integratedTerminal", hará que el programa se ejecute en la terminal integrada de VSCode.
*/

{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (console)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "dotnet: build",
            "program": "${workspaceFolder}/bin/Debug/net8.0/CSharp.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}